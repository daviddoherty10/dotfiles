" Set compatibility to Vim only
set nocompatible

" Automatically wrap text that extends beyond the screen lenght
set wrap

"force encoding
set encoding=utf-8

" Show line  numbers
set number

" Status Bar
set laststatus=2

" easy quit
inoremap kj <Esc>

syntax enable

